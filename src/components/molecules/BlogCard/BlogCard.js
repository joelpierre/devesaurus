import React from 'react';
import PropTypes from 'prop-types';

const BlogCard = props => {
  return <div data-test="component-blog-card" />;
};

BlogCard.defaultProps = {};

BlogCard.propTypes = {};

export default BlogCard;
