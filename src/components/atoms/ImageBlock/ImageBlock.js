import React from 'react';
import PropTypes from 'prop-types';

const ImageBlock = (props) => {
  return (<div data-test="component-image-block"/>);
};

ImageBlock.defaultProps = {};

ImageBlock.propTypes = {};

export default ImageBlock;
